project:
  goal: <项目目标,一句话>
  acceptance:
  - <验收标准 1>
  - <验收标准 2>
tasks:
- id: T1
  title: <首个任务标题>
  status: todo
  assignee: implementer
  deliverables: []
- id: T2
  title: 第二次启动 htc_main_app -m 卡在 RTSP 初始化,未到监听端口日志
  status: done
  assignee: implementer
  deliverables: []
- id: T3
  title: 第三次启动 htc_main_app -m 仍卡死(T2 teardown 修复后)
  status: done
  assignee: implementer
  deliverables: []
- id: T4
  title: 编写 MCU.h 全量 API 测试程序：先穷举读，再写+读回验证（仅 T32 真机）
  status: done
  assignee: implementer
  deliverables: []
- id: T5
  title: WiFi 驱动/连接状态复用:重启不再重复 insmod/重连(根治 DbusProcess oops)
  status: done
  assignee: implementer
  deliverables: []
- id: T6
  title: WiFi 连接管理独立应用:driver 单次加载+按参数/MCU 连接+DHCP+可选回写 MCU
  status: done
  assignee: implementer
  deliverables: []
- id: T7
  title: 扩展 htc_wifi_app 为 htc_net_app:统一网络管理(WiFi+Ethernet+USB dongle),本阶段不动 main_app
  status: done
  assignee: implementer
  deliverables: []
- id: T8
  title: 'Work-mode(-wm) extraction Phase A: SDK+thin-app direction doc & capability/work-mode
    inventory'
  status: done
  assignee: implementer
  deliverables: []
- id: T9
  title: 'Phase B-1: extract generateDescInfo/createDescInfoFile into a manifest library
    (behavior-preserving)'
  status: done
  assignee: implementer
  deliverables: []
- id: T10
  title: 'Root-fix: declare crc16 dep on common_utils_crc; drop manifest workaround'
  status: done
  assignee: implementer
  deliverables: []
- id: T11
  title: 'Phase B-2: sink app-inline orchestration into SDK libs (NTP wait-loop, buildMdnsParams,
    RTSP start/run/stop)'
  status: done
  assignee: implementer
  deliverables: []
- id: T12
  title: 'Phase B-3: lift CMD_RTSP_SERVER start/run/stop into app_workmode (signal
    machinery via DI, behavior-preserving)'
  status: done
  assignee: implementer
  deliverables: []
- id: T13
  title: 'Root-fix: declare daynight dep on media_rtsp (fix htc_media_app link break)'
  status: done
  assignee: implementer
  deliverables: []
- id: T14
  title: 'Phase C-1: extract app_lifecycle lib (ProcessLifecycle: signal machinery
    + common startup/shutdown); htc_main_app uses it, behavior-preserving'
  status: done
  assignee: implementer
  deliverables: []
- id: T15
  title: 'Phase C-2: extract -wm execution into app_workmode::runWorkMode/runCommands
    (behavior-preserving); htc_main_app calls it'
  status: done
  assignee: implementer
  deliverables: []
- id: T16
  title: 'Phase C-3: create thin htc_workmode_app (uses app_lifecycle + app_workmode::runWorkMode);
    exists but unspawned'
  status: done
  assignee: implementer
  deliverables: []
- id: T17
  title: 'sample-Encoder-jpeg-no-thumb: JPEG snap without CH2 thumbnail'
  status: done
  assignee: implementer
  deliverables: []
- id: T18
  title: 'quickSnap boot 首程序（media_app 原地重构，spec: doc/knowledge/specs/quicksnap-app-spec.md）'
  status: todo
  assignee: implementer
  deliverables: []
- id: T19
  title: wm 配合 quickSnap lean 模式：-m 3 heartbeat + lean 启动(无卡/无DB) + m2 /tmp 上传
  status: done
  assignee: implementer
  deliverables: []
- id: T20
  title: 'quickSpin spawn wm 前同步4G准备: spawnAndWait htc_net_app(--usb-bringup), 退出码非0则中止不spawn
    wm'
  status: todo
  assignee: implementer
  deliverables: []
- id: T21
  title: 'quickSnap 配置驱动上行: quicksnap.json network 字段(4g/wifi), prepareNetwork 按 config
    选 htc_net_app --type(测试期 wifi 替 4g)'
  status: todo
  assignee: implementer
  deliverables: []
- id: T22
  title: 'wm 上传失败 SD 落卡 + 下次启动续传（spec: doc/knowledge/specs/wm-upload-sd-fallback.md）'
  status: done
  assignee: implementer
  deliverables: []
- id: T23
  title: 'config.ini→JSON Phase-1: DeviceConfig ini→JSON 后端切换(行为等价 + golden)'
  status: done
  assignee: implementer
  deliverables: []
- id: T24
  title: 'config.ini→JSON Phase-2: 抽 ProductConfig (BOOT + DEVICE 静态字段; PID 留 DeviceConfig
    因 MCU-mirror)'
  status: done
  assignee: implementer
  deliverables: []
- id: T25
  title: 'config.ini→JSON Phase-3: setting.json 吸收用户字段 (timezone/bitrate/电压/UPID-UPWD)
    + test fixture'
  status: done
  assignee: implementer
  deliverables: []
- id: T26
  title: 'config.ini→JSON Phase-4: 清场+三桶成型 (rename config.json→system.json; 退役 env.ini/update_config.ini;
    删旧 .ini; constexpr 路径; install.sh; R4 EXDEV)'
  status: done
  assignee: implementer
  deliverables: []
- id: T27
  title: rename htc_net_app binary/target → net (unify with wm/um/quickSnap naming)
  status: done
  assignee: implementer
  deliverables: []
- id: T28
  title: um 能力自报(product.json um_*) + lean residentMode + HTTP/mDNS gate + fail-safe
    lean default
  status: done
  assignee: implementer
  deliverables: []
- id: T29
  title: um RTSP 改走 CH0-scaled-720p 单流(官方 nrvbs 指南) + capability-driven 通道映射，修 64MB
    OOM
  status: done
  assignee: implementer
  deliverables: []
decisions: []
risks:
- '{''key'': ''T2-hw-regression-unverified'', ''severity'': ''medium'', ''description'':
  ''静态链路 + 双平台编译已过,但 T32 硬件真实 IMP 驱动下第二次启动是否不再卡在 configure() 尚未在设备验证,留给用户做设备侧回归。''}'
- '{''key'': ''T3-hw-regression-pending-user'', ''severity'': ''medium'', ''description'':
  ''双平台编译已过，连续 5 次 kill+重启(第3次卡死)回归待 T32 硬件执行(imp_stub 全 no-op，PC 无真实 IMP 驱动)；analyst
  §8 已给脚本，留用户。''}'
- '{''key'': ''T3-hw-regression-pending-user'', ''severity'': ''medium'', ''description'':
  ''双平台编译已过，连续 5 次 kill+重启(第3次卡死)回归待 T32 硬件执行(imp_stub 全 no-op，PC 无真实 IMP 驱动)；analyst
  §8 已给脚本，留用户。''}'
- '{''key'': ''T4-hw-unverified'', ''severity'': ''medium'', ''description'': ''reviewer
  复跑仅限 sim 冒烟 + 代码审计（PC 不能跑 MIPS）。真机 Phase1（read 实数据 range 分类）、 Phase2（write+回读，定位
  writeESOR_WID 2B 截断）、--restore 回路（写后回读≈快照）需 T32 硬件验证，留用户执行 （见 doc/knowledge/playbooks/mcu-api-test-runbook.md）。另
  writeGps 地址错配在真机会恒显 readback mismatch FAIL， 非 writeESOR_WID 同类 bug，runbook 应区分说明。''}'
- '{''key'': ''T5-hw-unverified'', ''severity'': ''medium'', ''description'': "PC
  侧验证齐备(双平台 build exit 0 + 代码审计 + grep 校验),但 T32 真机连跑回归(htc_main_app -m 连跑 ≥6 次:第
  2 次起出现 ''WiFi already connected, skip connectWifi'' + ''already has IP, skip DHCP'',无
  insmod/File exists/ctrl_iface exists/oops,dmesg 无 DbusProcess/robust-futex,退出后 pgrep
  wpa_supplicant + lsmod|grep 8189fs 仍在)需设备侧执行,见 evidence 第 9 节脚本。"}'
- '{''key'': ''T6-shell-injection-preexisting'', ''severity'': ''low'', ''description'':
  ''wifi_reconnect.cpp reconnectSSID 把凭据以 shell 拼接方式喂给 wpa_passphrase/wpa_cli set_network（:157/186/192），经
  Misc::popencall→popen/system→sh -c。含 shell 元字符的 ssid/password 可被注入。但该风险与既有 wpa_conn.cpp:105
  generate_wifi_config（裸 + 拼接同款凭据进同款 shell 路径） 等价，main_app.cpp connectWifi 也经此；凭据源为本地
  CLI/MCU 寄存器非远程可达。故 severity low， 归入后续 WiFi 凭据安全加固，不阻塞 T6 merge。''}'
- '{''key'': ''T6-hw-unverified'', ''severity'': ''medium'', ''description'': ''reviewer
  仅做代码审计 + sim 冒烟 + 产物 file 核验（PC 不能跑 MIPS）。真机 WiFi 实连回归（script/regress_wifi_real.sh
  用例 A-F：A 首连 / B 优雅切 SSID 且 wpa_supplicant PID 恒定 / C --write-mcu 回写 MCU / D 错密码
  exit 3 / E 无参读 MCU / F 5 轮 A↔B PID 恒定 + dmesg 无 oops）需 T32 硬件手动跑。关键不变量：用例 B/F 的
  wpa_supplicant PID 跨 SSID 切换恒定是 T5 不回归的唯一可观测证明。''}'
- '{''key'': ''T7-usb-no-start'', ''severity'': ''high'', ''description'': ''main_app
  的 USB 序列缺 start()/activateContextProfile()，照搬后 net_app 的 --type usb 同样激活不了 4G 数据
  context→拿不到 IP(usb0 无 carrier，udhcpc 超时→exit 4)。这是 main_app 既有 行为，非本任务引入的回归。缓解:net_app
  默认忠实搬运(不偷偷补)；--usb-bringup 开关(默认关)留给 用户显式补 start()；full §5 给出补 start() 的精确序列；真机回归脚本断言
  IP/gateway，失败即暴露。 本风险需用户在 implement 前拍板 --usb-bringup 默认值。''}'
- '{''key'': ''T7-usb-setmodel-unset'', ''severity'': ''medium'', ''description'':
  ''UsbDongle::model 默认未初始化(enum class 零值=EC20)。main_app 从不 setModel()，net_app 照搬同样不设；若实际硬件是
  EC200A/EG800K/RG255AA，preconfig 的 setDevCtrl(3,1) 分支(UsbDongle.cpp: 1088)与 setWakeupConfig
  分支会走错。属 main_app 既有缺陷。缓解:照搬不补；--usb-bringup 路径顺带 接受 --usb-model 参数(EC20/EC200A/EG800K/RG255AA)由用户显式设；标风险不擅自默认。''}'
- '{''key'': ''T7-eth-no-carrier-on-sim'', ''severity'': ''medium'', ''description'':
  ''sim 下 eth0 可能不存在或无 IP，startDHCP(eth0) 的 udhcpc 失败→exit 4，属预期(sim 无硬件)。 缓解:sim
  验收只验编译+纯逻辑单测+--help+决策；ETH 执行层真机回归脚本留用户跑。不卡 CI。''}'
- '{''key'': ''T7-rename-cmake-linkage'', ''severity'': ''medium'', ''description'':
  ''改名 wifi_app→net_app 涉及 CMake target(add_executable/set_target_properties/ target_link_libraries
  两处 sim/T32)+tests/CMakeLists(test_net_app_logic)+注释。漏改一处→构建断或 残留 wifi_app。缓解:full
  §7 给出精确联动清单；验收 grep 证实 build/bin 无 htc_wifi_app 残留且 htc_net_app 生成；test_net_app_logic
  全绿。''}'
- '{''key'': ''T7-network-lib-sim-deps'', ''severity'': ''medium'', ''description'':
  ''net_app 要 link network 库(UsbDongle)，network 依赖 setting/env/disk/mcu/rtc/timezone/
  power/jsoncpp/md5/common_misc/common_utils_serial(network/CMakeLists.txt:37)。sim
  侧需确认这些 stub 库都在(common_utils_serial 在 sim 是否有 stub 要确认)。缓解:参照 htc_main_app sim link
  块(app/ CMakeLists.txt:102-150 已含 network)的依赖闭合；sim 编译验证；若 serial 无 stub，net_app
  仍可编( UsbDongle.cpp 调 SerialPort，但若 net_app 不编译 UsbDongle.cpp 只 link 预编 network.so，依赖由
  .so 自带)。net_app 只 link network(不重编 UsbDongle.cpp)，依赖链由 network.so 承载。''}'
- '{''key'': ''T7-wifi-equivalence-regression'', ''severity'': ''high'', ''description'':
  ''重构改名/新增分支可能误伤 WiFi 路径(connectWifi/reconnect/DHCP/writeback/exit code)。 缓解:WiFi
  分支代码原样搬到 net_app 的 runWifi()(只改 namespace/include/日志里的 app 名)；原 test_wifi_app_logic
  全部 case 迁到 test_net_app_logic 并保持断言；真机回归脚本(regress_wifi_real.sh) 换二进制名后重跑；--type
  wifi 默认即旧行为。''}'
- 'common_utils_crc (src/common/utils/crc/CMakeLists.txt) depends on cal_crc16 from
  the crc16 lib but does not link it; latent missing-dep masked because htc_main_app
  links crc16 directly. Follow-up: declare `target_link_libraries(common_utils_crc
  PUBLIC logger crc16)` at the source so consumers inherit it transitively instead
  of each consumer re-adding crc16 (as manifest now does).'
- 'media_rtsp (src/media/rtsp/CMakeLists.txt:36) uses DayNightSwitch (RtspServer.cpp:587-595,
  NOT BUILD_FOR_SIMULATION-gated) but does not link the daynight target. Latent missing-dep
  masked because htc_main_app links daynight directly; surfaces as a sim (and likely
  T32) link failure for htc_media_app (undefined DayNightSwitch::* refs). Same class
  as the crc16/T10 issue. Fix: target_link_libraries(media_rtsp PRIVATE ... daynight)
  at the source. Pre-existing, not introduced by T9-T12.'
- 'T14 (Phase C-1) PC-side verification complete (byte-identical refactor + dual-platform
  build + sim smoke incl. signal→cleanupHook→R1-gated-teardown), but the R1 risk (rtsp_singleton_used
  HAL-teardown gate drift → next cold boot hangs in IMP configure()) is ONLY verifiable
  on T32 hardware. USER-RUN Tier-3 device A/B required: cold-boot -m / -wm 3 (CMD_MOBILE)
  / -rs (CMD_RTSP_SERVER) vs HEAD build/bin/htc_main_app, confirm next boot does NOT
  hang in IMP configure(). Sim cannot surface this (Misc::poweroff no-op + _exit(0)).
  Do this before C5 retire; htc_main_app -wm remains the fallback/baseline through
  C4.'
- 'T17-R3 (medium, deferred-to-user): device capture unverified on PC (no MIPS runtime/real
  IMP driver). User must run on T32 and confirm /mnt/sdcard/DCIM/snap-12-2560x1440-{0..19}.jpg
  only, zero snap-14-*. Pre-retire gate.'
- 'T17-R4 (low, process): reviewer + tester subagents could not spawn (provider 429
  token-plan quota); PM executed both gates inline with full evidence. No verdict
  impact.'
- '{''key'': ''T19-RV-m2-tmp-scan-broad'', ''severity'': ''moderate'', ''description'':
  ''m2 lean 下 UploadTask 扫描根 = /tmp（wm_scheduler.cpp:216 + upload_task.cpp:199）。 uploadOneDesc
  对 /tmp 直属 json 文件按 PID 不匹配 / 缺 file_inf / 解析失败 / 非法 json 会静默 Misc::deleteFile（upload_task.cpp:244-262,
  344-345）。专用设备 tmpfs 上现实暴露低 （通常只有刚写的 desc），但比专用子目录（如 /tmp/wm_upload/）宽。spec §2.1
  line 64 与 §8 line 250 自相矛盾（前者说 /tmp，后者说 /tmp/media/），实现取 §2.1。建议后续收窄扫描根 + 对齐 spec。不阻塞合入。''}'
- '{''key'': ''T19-RV-spec-skipfactory-silent'', ''severity'': ''low'', ''description'':
  ''spec §2.1 lean 差异表未列 skipFactoryConfig/skipUpdateConfig，但实现额外设了这两个 （wm_app.cpp:201-202，planner
  R2-2 决策，避免 lean 下访问 SD）。建议 spec §2.1 表补行。''}'
- '{''key'': ''T19-HW-unverified'', ''severity'': ''medium'', ''description'': ''真
  no-SD（HW mountSDCard non-fatal 分支）、真后端 upload/heartbeat 成功、IMP-not-init 在真机、tmpfs
  占用——sim 测不全（sim mountSDCard 递归 mkdir 总成功）。需 T32 硬件回归 （planner 已标，留用户）。sim 验收 + 双平台编译
  + 本审查 code-level 核实已足够放行合入。''}'
- 'T28-hw-imp-channel-build-unverified (medium): cap→通道映射的 IMP 实际构建（IMP_Encoder_CreateChn/IMP_FrameSource_EnableChn/Bind）、首产品
  um_live-only 只建 CH1 的内存省量（~2-4MB）与 CMA 占用，只能在 T32 真机验证（PC sim imp_stub 全 no-op）。映射决策有
  R1-R4 单测覆盖（逻辑层）；回滚点 D（IngenicVideo.cpp um cap 分支 + HalProvider + um_app 接线）可单独 revert
  回 residentMode<0 全建。设备验证：I1 um 启动 log 审计（CH1 建、CH0/12/14 不建、dmesg 无 IMP 错）；I2 cat
  /proc/meminfo Cma/mem 对比 lean 前后。'
- 'T28-hal-bind-before-enable-regression (medium): um_app prewarm 门控 + 新增 HalProvider::start(caps)
  调用可能扰动 IMP lifecycle 序列（reviews/2026-06-24-wm-slice2-capture.md cm==1 根因 = Bind
  落在 Enable 后致 JPEG polling 超时）。首产品 um_snap OFF → prewarm 不调（减少扰动）；RTSP EnableChn
  前通道构建序列保持不变。设备验证：I3 若 dev 全开模板（um_snap ON），拍照 JPEG polling 不超时。'
- 'T28-productconfig-load-fail-not-failsafe (low): ProductConfig.cpp:25,32 的 early-return
  false 路径（文件缺失/JSON 坏）未 fail-safe——caps_set_ 保持空（非 {um_live}）→ cfg.caps.empty() →
  走 wm 全建分支（=legacy 行为，非 lean）。首产品 G1 golden test 保证 product.json 必含 capabilities
  且存在，实际不触发；corrupt-config 退化为 legacy 全建是保守安全方向。follow-up 可选：load 失败也走 {um_live} fail-safe。'
- 'T28-halprovider-log-no-caps (low): HalProvider.cpp:70 的 start log 不含 caps 内容，调试时无法从该行看出
  lean/全建。um_app.cpp:246 有独立 caps log 补偿。可选 follow-up：HalProvider log 加 caps 摘要。'
- 'T28-thumbnail-route-gate-limitation (low): GET /api/v1/camera/thumbnail?file_path=
  是双语义路由（snap 后取刚拍 thumb + 浏览已有 thumb），mongoose 按 URI prefix 注册无法按 query-param gate，故路由级留
  um_snap。若未来出 um_pb-only 产品（能浏览但不拍照），纯浏览缩略图须走 database/thumbnail（已在 pb gate）或拆 /thumbnail
  为 browse/live 两条路由。首产品 [um_live] 两者都关，无功能影响。'
- 'T29-hw-ch0-scaler-720p-unverified (high): CH0+Scaler→720p 单流是真机 IMP 行为，sim (imp_stub
  no-op) 测不出。核心假设：CH0 FS scaler 从 sensor 2560×1440 降到 1280×720 输出，raw ring 按 720p
  分配（非全分辨率）。设备复测：重跑 tools/mem_profile_um.sh，期待 maps-burst 的 11×8MB 块消失、MemFree >20MB
  稳态、trend.csv VmSize <<200MB、无 OOM。fall-back：保留 CH0 全分辨率 + 降 nrVBs(cmdline T30) +
  不建 CH1。'
- 'T29-rtsp-ch0-stream-unverified (high): RTSP 走 CH0(group0) 实际出帧 720p、帧率正常、持续 >5min
  无断流。sim RTSP 用 SimVideo 不读 stream_id。设备复测：VLC/ffprobe 连 rtsp://<device>:8554 确认
  1280×720 SDP + 出帧 + 帧率。'
- 'T29-rtsp-ch0-fov-unverified (high, loopback#1 引入): CH0 purePreview picWidth 已回
  sensor 原生 2560（全 FOV 降采样，非中心裁剪）。设备肉眼确认 RTSP 预览是全 sensor 视场角、非 2× 数码变焦。preBindAllChannels(IngenicVideo.cpp:1517-1528)
  读 picWidth 当 crop 源——若设备见变焦，回查 crop 逻辑。'
- 'T29-wm-bytes-identical-regression (high): buildChannels/fillFsAttrForOutput 签名变化，caps
  空分支(wm/quick_snap) sim 代码审计 + tester/reviewer 独立复审逐字等价。设备验证：启 wm + 拍照 + 录影各一次不回归。'
- 'T29-osd-double-prepare (medium): RTSP 走 CH0(group0) 时 IspOsdManager 双重 prepare（preBindAllChannels:1532
  + IngenicVideoStream::configure:1261）。代码审计幂等（ensureRegion timeHandle_>=0 守卫 + start
  started_.exchange(true) 守卫）。设备确认无 ERROR log。'
- 'T29-nrvbs-cmdline-followup (medium): 应用侧瘦身（砍 user-space 11×8MB）不回收内核 cmdline nrvbs
  为 CH0 全分辨率预留的 24MB /dev/rmem。MemFree 可能仍偏低。follow-up T30：查设备 /proc/cmdline，调低 nrvbs
  匹配 720p。'
- 'T29-doc-cleanup-ivideo-comment (low): src/hal/include/IVideo.h:102-103 注释仍写 um_live→CH1（T28
  映射），T29 已改 CH0。非阻塞，建议顺手清。'
active_task: T29
